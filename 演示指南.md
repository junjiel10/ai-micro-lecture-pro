# 课堂演示指南（3 分钟版 / 8 分钟版）

> 面向教研分享与公开演示，两种时长任选。

---

## 一、演示前 5 分钟准备

```powershell
# ① 在 VS Code 中打开 ai-micro-lecture-pro 文件夹
# ② 终端执行环境自检（有绿色 ✅ 就可以讲了）
.\.venv\Scripts\python.exe check_env.py

# ③ 启动服务（保持这个终端开着）
.\.venv\Scripts\python.exe app.py
```

浏览器打开 http://127.0.0.1:8000
### 可选：接入大模型（推荐在演示前 2 分钟做好）

打开创作台 → 右上角 **⚙ 大模型设置** → 填入你的 DeepSeek API Key → 保存并生效。
保存后页面右上角会从「内置教学脚本引擎」变成「大模型（deepseek-chat）」。

> 不想现场填 Key 也没关系：不配置也能完整跑通，只是脚本由内置引擎生成。
> 可以在演示前先填好，现场对比两种模式的效果差异 —— 这本身就是一个很好的讲点。
> 建议提前跑一次生成，让「最近作品」里有内容 —— 现场演示时可以先用历史作品讲效果，
> 再现场跑一次讲过程，避免冷场等待。

---

## 二、3 分钟精简版（只讲一条主线）

| 时间 | 做什么 | 说什么 |
|---|---|---|
| 0:00–0:30 | 停在**首页** | 「这个网站解决一个问题：老师想做个微课，但没有时间写脚本、录音、做画面、剪片子。这里只需要输入一个知识点。」 |
| 0:30–0:50 | 滚动到**架构图** | 「架构是：用户输入 → 编排层 → 7 个 Agent → 成片输出。编排层用固定工作流，每个 Agent 职责单一，最后还有质检闭环，不合格会自动回退重做。」 |
| 0:50–1:10 | 点进**创作台**，打开一个历史作品 | 「这是刚才生成的成片，带配音、背景音乐和中文硬字幕，可以点分镜卡片跳转回放。」 |
| 1:10–2:30 | 现场输入「翻转课堂」→ **开始制作** | 「现在现场跑一次。注意右边：7 步流水线在逐个点亮，日志里每一步都有思考、行动、观察三段。」<br>「④ 配音用的是 edge-tts；⑤ 画面是 Pillow 直接渲染 1080P 幻灯片，不需要装 LibreOffice；⑥ 用 FFmpeg 合成并烧录字幕。」 |
| 2:30–3:00 | 完成后看**质检报告** + 点「导出视频」 | 「质检 8 项全过，100 分。全程我只做了一件事：输入四个字。」 |

---

## 三、8 分钟完整版（含文档模式与人工干预）

### 第 1 段（0:00–1:30）问题与定位
- 首页开场：一句话讲清痛点与目标（零干预 / 少量干预）。
- 强调**可视化**：7 个 Agent 不是黑盒，每一步都有日志。
- 讲**两种模式**的区别：主题驱动 vs 文档驱动。

### 第 2 段（1:30–3:30）主题驱动全自动，现场跑
- 输入「布鲁姆教育目标分类学」，时长选「约 3 分钟」，配色选「晨光橙」。
- 边跑边讲 7 步：
  - ② 结构层：定大纲、拆要点、排分镜（8~12 个）
  - ③ 脚本层：逐镜写口语化旁白
  - ④ 配音 + SRT 字幕时间轴 + numpy 合成的背景音乐
  - ⑤ 七种版式自动排布的教学幻灯片
  - ⑥ 逐镜合成 → 拼接 → 混音 → 烧字幕
  - ⑦ 质检 8 项，不合格自动回退

### 第 3 段（3:30–5:00）文档驱动模式
- 点「选择文件」，上传 `examples/翻转课堂-教学改革说明.txt`（也可以现场用一份自己的教案 .docx）。
- 讲：**解析 → 提炼关键词与关键句 → 按教学逻辑重组**，旁白直接来自文档内容。
- 生成完成后，点开某一镜的分镜卡片，看旁白与文档的对应关系。

### 第 4 段（5:00–6:30）少量干预点（这是加分项）
- 点分镜卡片右下角的 **✎**，打开「修改后重做」。
- 改掉其中一镜的旁白 → 选一个新配色 → 点「开始重做」。
- 讲关键设计：**只重跑受影响的环节** ——
  改文案只重做该镜的配音与字幕，换配色只重渲染画面，其余复用，最后统一重新剪辑并再次质检。
- 重做完成后，质检报告会显示「第 2 轮」。

### 第 4.5 段（6:30–7:00）竖版短视频 + 自动插图
- 把「画面比例」从「横版 16:9」改成「**竖版 9:16**」，「插图」保持「自动生成插图」。
- 现场再跑一个短一点的（2 分钟），或者直接打开已生成的竖版作品。
- 讲两个点：
  1. **竖版不是裁一刀**：横向的流程、对比会自动改成纵向堆叠，字幕字号与底部边距
     重新计算（避开手机端 UI 遮挡）；
  2. **插图不是 AI 生图**：画面里的概念结构图、流程链、标签云是按“关系/流程/分类”
     模板程序化绘制的，文字全部来自脚本要点，不会把汉字画错、也不会编造内容。

### 第 5 段（7:00–7:40）看产物
- 打开 `output/<项目号>/` 目录，展示完整产物链：
  `plan.json`（脚本）/ `frames/`（画面）/ `shots/`（配音）/ `narration.srt` 与 `narration.ass`（字幕）/ `final.mp4`（成片）
- 讲一句：**每一步的产出都落盘、可检查、可复用**，这也是「Plan 模式固定工作流」的价值。

### 第 6 段（7:40–8:00）技术说明与收尾
- 三句话讲清技术选型（README 第十一节有表格）：
  1. 画面用 Pillow 直接画，不依赖 LibreOffice —— 这是能在普通学生电脑上跑通的关键；
  2. FFmpeg 由 pip 包自带，不需要管理员权限装软件；
  3. 没有大模型 Key 也能完整使用，内置教学脚本引擎会如实标注生成方式，不伪装成 AI。

---

## 四、常见提问与回答（预演）

**Q：这就是把 PPT 转成视频吗？**
不是。PPT 是「人做的幻灯片」，这里是 Agent 根据教学内容自己决定讲什么、怎么分镜、
每个分镜用什么版式，再自己配音和剪辑。

**Q：没有大模型怎么生成脚本？**
用的是内置教学脚本引擎：命中学科知识骨架就用精编内容，未命中就用通用教学叙事骨架
＋领域词库扩写；上传了文档则走文档驱动，直接从文档里提炼要点。
界面会明确显示当前是哪种方式。

**Q：插图是哪来的？为什么不用 AI 生图？**
插图由 `agent/illustrate.py` 用代码绘制（概念结构图、流程链、标签云、对照板），
文字全部取自脚本要点，因此不会出现文生图常见的「汉字画错、内容编造」。
如果确实需要写实配图，`.env` 里可以另配 `IMAGE_API_KEY` 走外部文生图接口。

**Q：质检不合格会怎样？**
ReviewAgent 会给出具体哪一项不合格，然后**只重做那一部分**（比如某镜配音过短就只重配音），
再重新合成并二次质检，日志里能看到 V1 → V2。

**Q：为什么不用 LangGraph / AutoGen？**
设计图里它们是参考方案。本项目的编排层自己实现了等价的状态机与反思闭环，
好处是零额外依赖、可在普通环境跑通；接口（`emit` 事件流）是通用的，
后续要换成 LangGraph 只需替换编排层。

---

## 五、如果现场出问题

| 现象 | 处理 |
|---|---|
| 配音没有声音 | 检查网络；断网会自动降级到 Windows 内置语音 |
| 生成很慢 | 把「目标时长」调到 2 分钟再演示 |
| 网页打不开 | 确认启动终端没有关闭；重跑 `start.bat` |
| 想跳过等待 | 直接用「最近作品」里已生成的项目讲解，最后再补跑一次 |

> 保险做法：演示前把「最近作品」准备好 3 个不同主题、不同配色的成片，
> 万一现场网络异常，也能完整讲完。
